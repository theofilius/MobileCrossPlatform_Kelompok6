// app/(tabs)/spacer.tsx
// File ini sengaja kosong — hanya placeholder untuk spacer di tab bar
// Navigasi ke screen ini tidak akan pernah terjadi
import { View } from 'react-native';
export default function SpacerScreen() {
  return <View />;
}